# BBX MCP Servers Configuration
# Verified working MCP servers

servers:
  # ==========================================================================
  # BBX Test Server (built-in for testing)
  # Tools: echo, timestamp, add, json_format
  # ==========================================================================
  test:
    transport: stdio
    command: ["python", "examples/test_mcp_server.py"]
    description: "BBX test server - echo, timestamp, add, json_format"

  # ==========================================================================
  # Filesystem - Official MCP Server (WORKING)
  # Tools: read_file, write_file, list_directory, search_files, etc.
  # ==========================================================================
  filesystem:
    transport: stdio
    command: ["npx", "-y", "@modelcontextprotocol/server-filesystem", "."]
    description: "File operations - read, write, list, search"

  # ==========================================================================
  # Puppeteer - Browser automation (requires puppeteer)
  # Tools: navigate, screenshot, click, type, etc.
  # ==========================================================================
  puppeteer:
    transport: stdio
    command: ["npx", "-y", "puppeteer-mcp-server"]
    description: "Browser automation with Puppeteer"

  # ==========================================================================
  # GitHub - Requires GITHUB_TOKEN env var
  # Uncomment when you have a token
  # ==========================================================================
  # github:
  #   transport: stdio
  #   command: ["npx", "-y", "@modelcontextprotocol/server-github"]
  #   env:
  #     GITHUB_TOKEN: "${GITHUB_TOKEN}"
  #   description: "GitHub API - issues, PRs, repos"

  # ==========================================================================
  # Notion - Requires NOTION_API_KEY
  # ==========================================================================
  # notion:
  #   transport: stdio
  #   command: ["npx", "-y", "@notionhq/notion-mcp-server"]
  #   env:
  #     NOTION_API_KEY: "${NOTION_API_KEY}"
  #   description: "Notion API - pages, databases"
